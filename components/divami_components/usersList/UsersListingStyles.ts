import { styled } from "@mui/system";
import { ProjectUsersList } from "../project-users-list/ProjectUsersList";

export const ProjectUsersListStyled = styled(ProjectUsersList)({
  width: "100%",
});
