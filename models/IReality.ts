export interface IReality {
    _id: string;
    job: string;
    capture: string;
    snapshot: string;
    completedDateTime: Date;
    status: string;
    type: string;
    mode: string;
    context?: string;
}